import 'package:flutter/material.dart';

class CarShopList extends StatelessWidget {
  const CarShopList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
